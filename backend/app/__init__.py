"""Financial Report Analyzer — stock-research-focused extraction engine.

The package is organized into the product's layers, each with a single
responsibility and a clean boundary:

    ingestion → parsing → extraction → normalization
    → sourcemap → search → summary → api
"""

__version__ = "0.1.0"
